#!/bin/bash

# Launch the TUI command passed in as an argument in the default terminal with an org.mistletoe.COMMAND app id for styling.

exec setsid xdg-terminal-exec --app-id=org.mistletoe.$(basename $1) -e "$1" "${@:2}"
