#!/bin/bash

# Overwrite the user configs for swayosd (controls on-screen feedback for changing volume/songs etc) with the Mistletoe defaults and restart the service.

mistletoe-refresh-config swayosd/config.toml
mistletoe-refresh-config swayosd/style.css
mistletoe-restart-swayosd
