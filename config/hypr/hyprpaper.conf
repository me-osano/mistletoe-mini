# Preload wallpapers
preload = ~/.config/mistletoe/current/background

# Set initial wallpaper (use monitor name or , for all monitors)
# Find monitor names: hyprctl monitors
wallpaper {
    path = ~/.config/mistletoe/current/background
    fit_mode = cover
}

# Splash screen (set to false for seamless boot)
splash = false
ipc = on